[package]
name = "agent-state-long"
version = "0.1.0"
edition = "2024"
description = "长程工作状态（独立 crate，可选）—— LongTermWS 精细定义"

[dependencies]
agent-state = { path = "../agent-state" }
serde = { version = "1", features = ["derive"] }
